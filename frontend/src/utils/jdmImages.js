/**
 * JDM Authentic Images Mapper Utility
 * Maps product/car models to exact high-definition JDM photos.
 */
export const getJDMImage = (model, brand = '', defaultUrl = '') => {
  // Return database value directly
  return defaultUrl;
};
